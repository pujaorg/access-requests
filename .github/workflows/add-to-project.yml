name: Add Issue to Project

on:
  issues:
    types: [opened]

permissions:
  contents: read
  issues: write
  repository-projects: write

jobs:
  add-to-project:
    runs-on: ubuntu-latest
    steps:
      - name: Add issue to project
        env:
          GH_TOKEN: ${{ secrets.PROJECT_AUTOMATION }}
        run: |
          # Use the GitHub CLI to add the issue to the project
          # The owner is the organization name "pujaorg"
          gh project item-add ${{ github.event.issue.number }} \
            --url "${{ github.event.issue.html_url }}" \
            --owner "pujaorg"
